exp_name: lpn
save_dir: /media/data1/feihong/ckpt/ground_lpn

model:
  type: lpn
  use_angle: true
  use_heatmap: false
  use_text: false

train:
  batch_size: 6

eval:
  output_dir: eval_results/grounding/lpn
