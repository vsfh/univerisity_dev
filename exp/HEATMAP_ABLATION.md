# Heatmap ablation

From the repository root, run `bash exp/run_heatmap_ablation.sh`.
This trains and tests all 15 runs in the foreground, replacing only
`outputs/heatmap_lambda_box_0p5/` on each invocation.
See [the instructions](../ablation_heatmap/README.md).
