exp_name: lpn
save_dir: /media/data1/feihong/remote/ckpt/ground_lpn

model:
  type: lpn
  pretrained: true
  use_angle: true
  use_heatmap: false
  use_text: false

train:
  batch_size: 30

eval:
  output_dir: eval_results/grounding/lpn
